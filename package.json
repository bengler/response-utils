{
  "name": "@bengler/response-utils",
  "version": "0.6.2",
  "description": "A set of utitilties for response",
  "main": "./lib/index.js",
  "dependencies": {},
  "scripts": {
    "test": "npm run build && mocha -R spec && npm run lint",
    "build": "babel src --out-dir lib",
    "lint": "eslint src",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "eslint": "^1.10.3",
    "eslint-config-bengler": "^0.5.5",
    "expect.js": "^0.3.1",
    "mocha": "^2.4.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bengler/response-config"
  },
  "author": "Bengler AS"
}
